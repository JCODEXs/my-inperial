import { ImagePluginSettings } from "../../types";
declare const _default: (maxWidth: number, containerWidth: number, sourceWidth: number, sourceHeight: number, pluginSettings: ImagePluginSettings, nodeWidth?: number, nodeHeight?: number, nodeMaxWidth?: number) => {
    width: number;
    height: number;
};
export default _default;
