import { Node } from "prosemirror-model";
import { EditorView } from "prosemirror-view";
import { ImagePluginSettings } from "../../types";
declare const _default: (height: number, width: number, getPos: boolean | (() => number), node: Node, view: EditorView, image: HTMLImageElement, setResizeActive: (value: boolean) => void, maxWidth: number, pluginSettings: ImagePluginSettings) => HTMLDivElement;
export default _default;
