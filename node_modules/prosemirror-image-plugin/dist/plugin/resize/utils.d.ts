import { resizeDirection } from "../../types";
export declare const setSize: (element: HTMLElement, width: number, height: number) => void;
export declare const resizeFunctions: {
    [direction in resizeDirection]: (element: HTMLElement, width: number, height: number) => void;
};
